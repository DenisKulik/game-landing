import { AppWrapper } from 'app/app.styles.ts'
import { Header } from 'layout/header'

export const App = () => {
    return (
        <AppWrapper>
            <Header />
        </AppWrapper>
    )
}
